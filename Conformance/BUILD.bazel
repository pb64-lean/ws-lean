load("@rules_lean//lean:defs.bzl", "lean_binary")

package(default_visibility = ["//visibility:public"])

lean_binary(
    name = "echo_server",
    srcs = ["EchoServer.lean"],
    tags = ["manual"],
    deps = ["//:ws_lean"],
)

lean_binary(
    name = "load_client",
    srcs = ["LoadClient.lean"],
    tags = ["manual"],
    deps = ["//:ws_lean"],
)
